extends ../layout

block content
    h2 Generate knowledge check
    form(action='/kc/units')
        p Course ID:&nbsp;
            input(name='courseId' placeholder='ex: az-204')
        if message
          p.error Error: #{message}
        p Locale:&nbsp;
            input(name='locale' value='en-us')
        p
            label(for='fieldRaw') Output:&nbsp;
            select(name='format' id='fieldRaw')
                option(value='html') Knowledge Check
                option(value='outline') Outline
                option(value='json') Raw JSON
                option(value='klax') Poll Script
        p
            button(type='submit') Select Units

    small Please note: Generating a document may takes a few minutes. Please be patient.